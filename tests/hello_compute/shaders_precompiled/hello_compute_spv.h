// Placeholder - compile hello_compute.comp to generate real data
// This file exists so the build doesn't fail when no SPIRV compiler is available
#pragma once
#include <cstdint>
#include <cstddef>

inline const uint8_t hello_compute_spv[] = { 0 };
inline const size_t  hello_compute_spv_size = 0;
