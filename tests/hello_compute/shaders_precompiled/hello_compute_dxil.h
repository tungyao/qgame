// Placeholder - compile hello_compute.hlsl to generate real data
// This file exists so the build doesn't fail when no DXC compiler is available
#pragma once
#include <cstdint>
#include <cstddef>

inline const uint8_t hello_compute_dxil[] = { 0 };
inline const size_t  hello_compute_dxil_size = 0;
